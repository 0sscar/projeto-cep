/*
import React from 'react';
import { View, StyleSheet } from 'react-native';
import Modalogin from './src/components/modal_login/modal_login';

const App = () => {
  return (
    <View style={styles.container}>
      <Modalogin />
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default App;

seguinte, esse era o codigo do App.js original, no comeco criei esse arquivo achando que era só por o home aqui e iria funcionar
porém nada que jogo aqui funciona, tentei jogar o arquivo chamando o modal por aqui e também mas não deu certo, então só guardei ele 
aqui mesmo*/
